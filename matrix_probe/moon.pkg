import {
  "moonbitlang/parser",
  "moonbitlang/parser/syntax",
  "moonbitlang/parser/untyped_cst" @cst,
  "moonbitlang/lexer/basic",
}

supported_targets = "js"

pkgtype(kind: "executable")

import {
  "moonbitlang/core/test",
} for "wbtest"

options(
  link: { "js": { "format": "iife" } },
)
